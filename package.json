{
  "name": "be-yuumi-server",
  "version": "1.0.0",
  "description": "Backend server for BeYumm app",
  "main": "index.js",
"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "start": "node dist/server.js",
  "build": "parcel build server.js --target node --out-dir dist"

  },
  "author": "Mohit Paul",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^4.0.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}

